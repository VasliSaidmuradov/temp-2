export default async function({ store }) {
  const queue = []
  // const banners = store.getters['banner/GET_BANNERS'];
  // if (!banners) {
  //   queue.push(store.dispatch('banner/fetchBanners'))
  // }
  // await Promise.all(queue);
}
